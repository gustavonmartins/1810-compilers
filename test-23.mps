picture "my-PSBeispiel"

var x : Num;
var y : Num;
var z : Num;
var p : Path;
var black : Term;
var zabur : Path;
start               % Beginner mit Zeichenansweisungen

%x:=0.5;
%y:=0.6;
%z:=0.7;

setcolor(0.9,0.5,0.5);
setdrawstyle(6,3);

setfont("Arial", 12);
%setcolor(x,0.5,0.5);

%Goal: start inc end {/id exch def cmd } for
for q:=10 to 20 step 3 do setcolor(q,0.3,0.4); done;

setlinewidth(3);


end
